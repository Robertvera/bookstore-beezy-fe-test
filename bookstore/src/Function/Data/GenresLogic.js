const genreData = {
    genre: class Genre {
            constructor (id, name) {
                this.id = id,
                this.name = name
        }
    },

    genresArray: []
}

export default {genre, genresArray};